import ActivityKit
import Foundation

@MainActor
final class SecretGenerationLiveActivityManager {
//    private var activity: Activity<SecretGenerationActivityAttributes>?
    private var lastSentPercentage = -1

    func startActivity(for title: String) async {
        
    }

    func update(using generation: ActiveSecretGeneration) async {
        
    }

    func completeActivity() async {
        
    }
}
